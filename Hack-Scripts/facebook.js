/* fun script to accept friend request without using mouse */ 
for( i = 0;i<document.getElementsByName("actions[accept]").length;i++)
{
    document.getElementsByName("actions[accept]")[i].click();
    console.log("accept friend request")
 }void(0);
