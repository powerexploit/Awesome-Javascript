var num = 1;
setInterval(() => {
    //function code here
    console.log(num);
    num += 1;
}, 10000); //  <= can change time here <1000 miliseconds = 1 second>, so every 1 second it will plus number
          // it will go forever;
